# Sample Counter v5

In this example, we explore how to develop the counter application targeting SolidJS. We will define the model and 
stores in C#, and then let the SolidJS consume those stores to render the UI. The idea is to show how to use the 
SolidJS bindings to the SolidJS framework, and how to let most code be written in C#.